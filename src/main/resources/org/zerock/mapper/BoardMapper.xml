<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- xml를 활용한 공통코드 -->

<!-- mapper 인터페이스의 sql 처리용 코드 -->
<mapper namespace="org.zerock.mapper.BoardMapper"> <!-- 패키지.인터페이스명 -->

<select id="getList" resultType="org.zerock.domain.BoardVO"> <!-- id:메서드명 / resultType : 객체 -->
	<![CDATA[
		select * from tbl_board where bno > 0
	]]> <!-- CDATA 사용 이유는 부등호인지 문자인지 판단하지 말것 (문자처리) -->
</select>

<insert id="insert">
		<![CDATA[	
			insert into tbl_board (bno, title, content, writer)
			values (seq_board.nextval, #{title}, #{content}, #{writer})
		]]> 
</insert>

<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey> <!-- insert 전에 번호를 미리 만들어 놓음 -->
		
		<![CDATA[	
			insert into tbl_board (bno, title, content, writer)
			values (#{bno}, #{title}, #{content}, #{writer})
		]]> 
</insert>

<select id="read" resultType="org.zerock.domain.BoardVO">
		<![CDATA[	
			select * from tbl_board where bno = #{bno}
		]]> <!-- bno와 같은 테이블에 모든 정보를 가져와 객체화 시켜라 -->
</select>

<update id="update">
		<![CDATA[	
			update tbl_board set 
			title = #{ title }, content = #{ content }, writer = #{ writer }, updateDate = sysdate
			where bno = #{bno}
		]]> <!-- 번호를 가지고 해당하는 객체를 수정한다. -->
</update>

<delete id="delete">
	<![CDATA[
		delete from tbl_board where bno = #{ bno }
	]]> <!-- 번호를 조건으로 레코드(db의 1 줄을 뜻함)를 삭제한다. -->
</delete>

<!-- 페이징 처리 관련 -->
<select id="getListWithPaging" resultType="org.zerock.domain.BoardVO">
	<![CDATA[
		select
		bno, title, content, writer, regdate, updateDate
		from
		(
		select /*+INDEX_DESC(tbl_board pk_board) */ rownum rn, bno, title, content, writer, regdate, updateDate
		from
			tbl_board
			where
	]]>
	<trim prefix="(" suffix=") AND " prefixOverrides="OR">
		<foreach item="type" collection="typeArr">
			<trim prefix="OR">
				<choose>
					<when test="type == 'T'.toString()"> title like '%'||#{keyword}||'%' </when>
					<when test="type == 'C'.toString()"> content like '%'||#{keyword}||'%' </when>
					<when test="type == 'W'.toString()"> writer like '%'||#{keyword}||'%' </when>
				</choose>
			</trim>
		</foreach>
	</trim>
<![CDATA[ rownum <= #{pageNum} * #{amount} ) where rn > (#{pageNum} -1) * #{amount} ]]>
</select>

<!-- 페이징 처리 관련 2 -->
<select id="getTotalCount" resultType="int">

	select count(*) from tbl_board where bno > 0


</select>

</mapper>